// lib/colors.dart

import 'package:flutter/material.dart';

// Definisikan warna-warna sesuai kebutuhan Anda
const Color primary = Color.fromRGBO(1, 126, 216, 1);   // Warna biru (Primary)
const Color secondary = Color.fromRGBO(129, 193, 71, 1);   // Warna hijau (Success)
const Color danger = Color.fromRGBO(249, 88, 140, 1);    // Warna merah (Danger)
const Color warning = Color.fromRGBO(254, 184, 62, 1);   // Warna kuning (Warning)
const Color disable = Color.fromRGBO(193, 211, 219, 1);   // Warna kuning (Disable)
const Color info = Color(0xFF17A2B8);      // Warna biru muda (Info)
const Color light = Color(0xFFF8F9FA);     // Warna abu-abu muda (Light)
const Color dark = Color(0xFF343A40);      // Warna abu-abu tua (Dark)
